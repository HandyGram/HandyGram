import 'package:flutter/material.dart';

class ElementsPadding extends StatelessWidget {
  const ElementsPadding({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 4.7);
  }
}

class SectionsPadding extends StatelessWidget {
  const SectionsPadding({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 32);
  }
}

class SmallButtonPadding extends StatelessWidget {
  const SmallButtonPadding({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 15);
  }
}
